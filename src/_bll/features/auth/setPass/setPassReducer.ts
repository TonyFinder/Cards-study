let initialState: InitialStateType = {}

export const setPassReducer = (state: InitialStateType = initialState, action: SetPassActionTypes): InitialStateType => {
    switch (action) {
        default:
            return state
    }
}


// actions


// thunks


// types
export type SetPassActionTypes = {}
type InitialStateType = {}