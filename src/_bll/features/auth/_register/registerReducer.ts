let initialState: InitialStateType = {}

export const registerReducer = (state: InitialStateType = initialState, action: RegisterActionTypes): InitialStateType => {
    switch (action) {
        default:
            return state
    }
}


// actions


// thunks


// types
export type RegisterActionTypes = {}
type InitialStateType = {}